<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android4a">

    <application
        android:name=".injection.EsieaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.CreateAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATEACCOUNT" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.DetailActivity">
            <intent-filter>
                <action android:name="android.intent.action.DETAIL" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.PokemonMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.POKEMONMAIN" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>